import React from "react";
import { Camera } from "expo";
import { View } from "react-native";

// a header with photo options
// toggle on/off flash
// change HDR

export default class Photo extends React.Component {
  render() {
    return <View />;
  }
}
